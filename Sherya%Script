local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/memejames/elerium-v2-ui-library/main/Library", true))()
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualInputManager = game:GetService("VirtualInputManager")

local player = Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
player.CharacterAdded:Connect(function(c) char = c end)

local window = library:AddWindow("Sherya Script", {
    main_color = Color3.fromRGB(200, 0, 255),
    min_size = Vector2.new(660, 660),
    can_resize = false,
})

local mainTab = window:AddTab("Main")

local autoGolpe = false

mainTab:AddSwitch("Auto Golpe", function(state)
    autoGolpe = state

    local function equiparGolpe()
        local mochila = player:WaitForChild("Backpack")
        local herramienta = mochila:FindFirstChild("Golpe")

        if herramienta then
            herramienta.Parent = player.Character
            task.wait(0.2) -- Esperar a que se equipe
        end
    end

    -- Detectar cuando el personaje respawnea (muere o evoluciona)
    player.CharacterAdded:Connect(function()
        if autoGolpe then
            task.wait(1) -- Esperar a que cargue el personaje y mochila
            equiparGolpe()
        end
    end)

    task.spawn(function()
        while autoGolpe do
            pcall(function()
                local character = player.Character or player.CharacterAdded:Wait()
                local herramienta = character:FindFirstChild("Golpe") or player.Backpack:FindFirstChild("Golpe")

                if herramienta then
                    if herramienta.Parent == player.Backpack then
                        herramienta.Parent = character
                        task.wait(0.2)
                    end
                    herramienta:Activate()
                end
            end)
            task.wait(0.1)
        end
    end)
end)

local autoOrb = false
local orbGuiCreado = false

mainTab:AddSwitch("Auto Orb", function(state)
    autoOrb = state

    -- Solo crea el mensaje visual una vez
    if not orbGuiCreado then
        local screenGui = Instance.new("ScreenGui")
        screenGui.Name = "Sherya"
        screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

        orbGuiCreado = true
    end

    -- Auto Orb loop
    task.spawn(function()
        while autoOrb do
            for _, v in pairs(workspace:GetChildren()) do
                if v.Name == "Drop" and v:IsA("BasePart") then
                    v.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
                end
            end
            task.wait(1)
        end
    end)
end)

-- AUTO RENACER
local autoRebirth = false
mainTab:AddSwitch("Auto Renacer", function(state)
    autoRebirth = state
    task.spawn(function()
        while autoRebirth do
            pcall(function()
                -- Paso 1: Clic en botón RENACER
                local renacerBtn = player.PlayerGui:FindFirstChild("MainGUI") and player.PlayerGui.MainGUI:FindFirstChild("Rebirth")
                if renacerBtn and renacerBtn.Visible then
                    fireclickdetector(renacerBtn:FindFirstChildWhichIsA("ClickDetector"))
                    task.wait(1)
                end

                -- Paso 2: Escribir 'yes' en el cuadro de confirmación
                local confirmarGui = player.PlayerGui:FindFirstChild("ConfirmGUI")
                if confirmarGui and confirmarGui.Enabled then
                    local inputBox = confirmarGui:FindFirstChildWhichIsA("TextBox", true)
                    local confirmarBtn = confirmarGui:FindFirstChild("Confirmar", true)
                    if inputBox and confirmarBtn then
                        inputBox.Text = "yes"
                        VirtualInputManager:SendKeyEvent(true, "Return", false, game)
                        task.wait(0.3)
                        fireclickdetector(confirmarBtn:FindFirstChildWhichIsA("ClickDetector"))
                    end
                end
            end)
            task.wait(6)
        end
    end)
end)
